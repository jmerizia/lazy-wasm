range a b =>
    (if (equals a b)
        [ a ; nil]
        [ a ; (range (add a 1) b)]
    )

reduce f r => 
    (if (not (second r))
        (r)
        (f
            (first r)
            (reduce f (second r))
        )
    )

(print_int (reduce add (range 1 10)))
